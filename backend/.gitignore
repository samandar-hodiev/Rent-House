# Local environment — the repository root .gitignore also covers this.
.env

# Compiled binaries. Anchored with a leading slash so the pattern cannot also
# swallow the cmd/server package directory.
/bin/
/tmp/
/server
